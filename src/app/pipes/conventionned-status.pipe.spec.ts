import { ConventionnedStatusPipe } from './conventionned-status.pipe';

describe('ConventionnedStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ConventionnedStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
